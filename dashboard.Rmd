---
title: "video game stats"
output: 
  flexdashboard::flex_dashboard:
    orientation: columns
    vertical_layout: fill
runtime: shiny
---

```{r setup, include=FALSE}
library(flexdashboard)
library(shiny)
library(datasets)
#data(faithful)
##
# Authors: Justin Hathaway, Jenkin Schibel, Anthony Moran
##

# Take in our csv file and create a dataframe
games <- read.csv("games.csv", header=TRUE)
data(games)
```

Column {.sidebar}
-----------------------------------------------------------------------

Waiting time between eruptions and the duration of the eruption for the
Old Faithful geyser in Yellowstone National Park, Wyoming, USA.

```{r}
selectInput("n_breaks", label = "some stuff:",
            choices = c(10, 20, 35, 50), selected = 20)

sliderInput("bw_adjust", label = "Bandwidth adjustment:",
            min = 0.2, max = 2, value = 1, step = 0.2)
```

Column
-----------------------------------------------------------------------

### Geyser Eruption Duration

```{r}
renderPlot({
  hist(faithful$er, probability = TRUE, breaks = as.numeric(input$n_breaks),
       xlab = "publisher sales per platform", main = "number of games sold per console by publisher")
  
  dens <- density(count(games$Platform), adjust = games$Publisher)
  lines(dens, col = "blue")
})
```
